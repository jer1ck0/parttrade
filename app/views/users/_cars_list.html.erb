<ul><% @user.cars.each do |car| %>
<li><%= car.id %> <%= car.vendor %><%= car.model %> <%= car.vin %><%= car.year %> <%= car.mileage %> <%= link_to 'edit', edit_car_path(car.id) %><%= link_to 'delete', car,  method: :delete, data: { confirm: 'Are you sure?' } %>
</li>
<% end %>
</ul>
